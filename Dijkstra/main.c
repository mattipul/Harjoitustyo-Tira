#include <stdio.h>
#include <stdlib.h>
#include "structure.h"


int main()
{
/*
    dNode *v0,*v1,*v2,*v3;

    v0=malloc(sizeof(dNode));
    v1=malloc(sizeof(dNode));
    v2=malloc(sizeof(dNode));
    v3=malloc(sizeof(dNode));

    v0->distances_to[1]=10;
    v1->distances_to[0]=10;

    v0->distances_to[3]=1;
    v3->distances_to[0]=1;

    v0->distances_to[2]=2;
    v2->distances_to[0]=2;

    v1->distances_to[2]=2;
    v2->distances_to[1]=2;

    v2->distances_to[3]=3;
    v3->distances_to[2]=3;

    list[0][0]=v0;
    list[0][1]=v1;
    list[0][2]=v2;
    list[0][3]=v3;

    list[1][0]=v1;
    list[2][0]=v2;
    list[3][0]=v3;

    list[1][1]=v0;
    list[2][1]=v2;

    list[1][2]=v0;
    list[2][2]=v1;
    list[3][2]=v3;

    list[1][3]=v0;
    list[2][3]=v2;

    init(list);
    dijkstra(v0);

    printf("%d", v1->distance);

    free(v0);
    free(v1);
    free(v2);
    free(v3);
*/
    init_cmd();
    return 0;
}
